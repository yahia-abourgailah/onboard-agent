# Copy to .env for local development: cp .env.example .env
# Real values per environment live in the platform's secret store, never in git.

# One of: development | staging | production
ONBOARD_ENVIRONMENT=development
ONBOARD_DEBUG=true
